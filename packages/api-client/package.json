{
  "name": "@repo/api-client",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf ./src",
    "generate:client": "openapi-generator-cli generate -i ./openapi-spec.yaml -g typescript-axios -o ./src -c ./config.json",
    "generate": "npm run clean && npm run generate:client && npm run createEnum",
    "organize": "node scripts/organize-types.js",
    "createEnum": "node scripts/extract-enums.js",
    "validate": "openapi-generator-cli validate -i ./openapi-spec.yaml",
    "generate:docker": "rm -rf ./src && docker run --rm -v \"${PWD}:/local\" openapitools/openapi-generator-cli generate -i /local/openapi-spec.yaml -g typescript-axios -o /local/src -c /local/config.json && npm run organize"
  },
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.18.4"
  },
  "devDependencies": {
    "yaml": "^2.8.1"
  }
}
